#ifndef CONSTANTS_H
#define CONSTANTS_H

namespace constants {
    //Since the variables are defined in a namespace in inline.cpp,
    //forward declarations need to be in a namespace too.
    extern const double pi;
    extern const double myGravity;
}

#endif